<p id="notice"><%= notice %></p>

<h1>Products in the Store</h1>

<table class="table table-responsive">
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Image url</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.Title %></td>
        <td><%= strip_tags(product.Description) %></td>
        <td><%= image_tag(product.Image_Url) %></td>
        <td><%= product.Price %></td>
        <td><%= button_to 'Add to Cart', line_items_path(product_id: product) %></td>
        <td><%= link_to 'Show', product , class:"btn btn-xs btn-info" %></td>
        <td><%= link_to 'Edit', edit_product_path(product) , class:"btn btn-xs btn-info" %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } , class:"btn btn-xs btn-info" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
